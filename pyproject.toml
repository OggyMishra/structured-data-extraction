[project]
name = "structured-data-extraction"
version = "0.1.0"
description = "LLM based structured data extraction"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "dspy>=3.2.1",
]
